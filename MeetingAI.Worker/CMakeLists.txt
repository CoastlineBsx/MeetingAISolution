cmake_minimum_required(VERSION 3.20)

message(FATAL_ERROR
    "The CMake build is intentionally paused. "
    "MeetingAI currently supports Windows x64 through MeetingAISolution.sln "
    "and Visual Studio 2022/MSBuild only. See the repository README.")
